{
  "schema_version": 1,
  "run_id": "<run-id>",
  "change_id": "<change-id>",
  "stage": "<stage>",
  "role": "<role>",
  "objective": "<single bounded objective>",
  "required_output": "<artifact or one task>",
  "task_id": "<N.N for apply, otherwise null>",
  "required_reads": [],
  "allowed_paths": [],
  "forbidden_paths": [],
  "forbidden_actions": [],
  "acceptance": {
    "write_result": ".sdd/runtime/agent-result.json",
    "tests": {},
    "result_contract": {
      "commands_run": "array of argv arrays",
      "tests": "array of {command, exit_code}",
      "requirement_evidence": "array of requirement-to-implementation-and-test mappings",
      "residual_risks": "array of concrete remaining risks"
    }
  }
}
