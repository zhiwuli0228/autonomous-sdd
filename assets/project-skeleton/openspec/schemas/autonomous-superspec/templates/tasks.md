# Tasks

## 1. Implementation

- [ ] 1.1 Implement parameter-driven variable-length custom header payload support with focused tests
  - Map the slice to explicit spec scenarios.
  - Keep production and test work in the same task.
- [ ] 1.2 Preserve unpack correctness, original CLI compatibility, and unchanged build entrypoint with regression tests
  - Use plain bullets for acceptance details; never create nested checkboxes.
- [ ] 1.3 Deliver the tool skill for THX/header inspection and verify end-to-end behavior

Create 3-20 numbered top-level tasks total. Every task MUST match `- [ ] N.N Title`.
Use plain nested bullets for details. Do not create any other checkbox.
